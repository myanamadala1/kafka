apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  labels:
    app: {{ template "kibana.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "kibana.fullname" . }}-network-policy
spec:
  podSelector:
    matchLabels:
      app: {{ template "kibana.name" . }}
      release: {{ .Release.Name }}
  ingress:
  - {}