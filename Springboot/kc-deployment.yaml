apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: kafkac-eai-deployment
  namespace: garagetest
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: kafkac-eai-app
    spec:
      containers:
      - name: kafkac-eai-container
        image: "reg-dhc-americas.app.corpintra.net/myanama/kafka-consumer:1.0.0"
        ports:
        - containerPort: 9004
      securityContext:
        fsGroup: 9999
        runAsUser: 1000
        supplementalGroups:
        - 9999
---
apiVersion: v1
kind: Service
metadata:
  name: kafkac-eai-service
  namespace: garagetest
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 9004
  selector:
    app: kafkac-eai-app
---
kind: NetworkPolicy
apiVersion: extensions/v1beta1
metadata:
  name: kafkac-eai-networkpolicy
  namespace: garagetest
spec:
  podSelector:
    matchLabels:
      app: kafkac-eai-app
  ingress:
    - ports:
      - port: 9004
        protocol: TCP
    - from:
      - namespaceSelector:
          matchLabels:
            name: ingress
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: kafkac-eai-ingress
  namespace: garagetest
  annotations:
    traefik.frontend.rule.type: PathPrefixStrip
spec:
  rules:
  - http:
      paths:
      - path: /qa/kafka2
        backend:
          serviceName: kafkac-eai-service
          servicePort: 80
